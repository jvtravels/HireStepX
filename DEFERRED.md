# Deferred Items Ledger

This file tracks intentionally-deferred work. Append-only (add a "resolved"
note inline when shipped — don't delete the line). Sweep monthly.

## Format
- `[YYYY-MM-DD | commit-sha] Title — brief reason for deferral. (resolved: yyyy-mm-dd sha if shipped)`

## Architecture
- [2026-05-15 | dabb3ea] Market-mode auto-inference from role — currently set explicitly via `initState`; `inferMarketMode` + `getConcessionMultiplier` are authored in `_market-mode.ts` but never called by the engine. Auto-inference path (role=data → force-hot) not implemented.
- [2026-05-15 | e599526] Wave-5+6+7 mass flag expansion — ~63 of 73 originally-scoped candidate-profile flags unshipped; polish, not blocking.
- [2026-05-15 | 5d841b5] Equity-instrument flags (Fix 6 in 2026-05-09 PDF) — cut due to scope of the candidate-profile enumeration pattern; system prompt does carry an EQUITY DISCLOSURE rule as a partial mitigation.
- [2026-05-15 | 3fcac3d] Sprint C.2 active-phase gating in `derivePhase` — `derivePhase` exits "opening" the instant `highestOfferMade > 0`; the practical gate is the move-picker discovery-incomplete branch (shipped). A second pass through `derivePhase` itself remains deferred.

## Detectors authored but not wired
- [2026-05-15 | 4482f87] Trial-close detector (`detectTrialCloseAsked`, `detectTrialCloseResponse`, `detectVariableComfortAsked`, `analyzeEquityClarity` in `_trial-close-detector.ts`) — exported and unit-tested but never threaded into the brief / move-picker. Surfaces a sophisticated negotiation tactic class; wiring is its own ship.
- [2026-05-15 | 4482f87] Verbose recruiter-critique mode (`critiqueRecruiterWithQuotes`) — kept for the planned post-session coach surface; not used by the live engine.
- [2026-05-15 | 4482f87] `shouldDiscloseRange` in `_range-disclosure-phase.ts` — helper for the planned phase-rule consolidator; current range-disclosure path runs through the kernel phase enum directly.

## Tests
- [2026-05-15 | cb1aa75] Dedicated `[ITEM REFUSED — SKIPPED]` brief-injection test — covered transitively in `refusalFallback.test.ts`; no standalone brief test.
- [2026-05-15] ~6 pre-existing flaky tests (verifyAuth retry + e2e-flows localStorage timing + wave3/wave4 system-prompt regex timeouts) — env flakes, unrelated to recent ships. Show up in some `vitest run` invocations and not others.

## Negotiation-flow redesign (2026-05-15 session — commits 3, 4, 6, 7, 8)
- [2026-05-15] Commit 3 — `planNextAction(state) → NextAction` central planner that owns the "what should the recruiter do this turn" decision (currently distributed across move-picker + brief + system-prompt + discovery-sequence). Cut: would require an audit pass over all 4 producers to make sure the planner doesn't drift from current behavior; one-shot risk too high for this session. `validateNextActionEmitted` (commit 5) was scope-cut to re-use `getNextDiscoveryQuestion` instead of consuming `planNextAction` output. (resolved: 2026-05-15 — `_next-action-planner.ts` shipped; move-picker is a thin shell; brief reads `state.plannedNextAction`; planner registers via globalThis hook to break the kernel↔planner load cycle. `validateNextActionEmitted` rewiring still pending — see entry below.)
- [2026-05-15] Commit 3 follow-up — `validateNextActionEmitted` consumed `getNextDiscoveryQuestion` rather than `state.plannedNextAction.ask`. (resolved: 2026-05-15 commit 3 — validator now reads `state.plannedNextAction.ask` when the planned action is `discovery-probe`, with a getNextDiscoveryQuestion fallback for back-compat with pre-commit-3 serialized sessions.)
- [2026-05-15] Commit 3 follow-up — `globalThis.__hr_nextActionPlanner` load-order workaround between kernel and planner. (resolved: 2026-05-15 commit 4 `30acc8c` — new `_planner-registry.ts` module breaks the cycle one-direction-style; kernel and planner both import the registry; the registry imports neither. globalThis hack fully removed.)
- [2026-05-15] Commit 4 — `lastBriefTags` → `briefDecisionLog: BriefDecisionEntry[]` ring buffer (last 5 turns of {tag, turnIndex, reason, accepted}). Cut: state-shape extension touches every reducer call site; without commit 3's planner the entries would be redundant with existing decisionLog. (NOTE 2026-05-15 session 3: commit 4 was reframed as reactive-followup rules per /tmp/negotiation-flow-audit.md C.2; that ship landed in `30acc8c`. The briefDecisionLog ring-buffer remains deferred — orthogonal to reactive rules.)
- [2026-05-15] Commit 4 (reactive followups) — shipped 2026-05-15 `30acc8c`. NextAction union extended with `reactive-followup`; planReactiveFollowup gate priority-runs above probe-mismatch. Rules: answer-direct, variable-comfort, competing-credibility, notice-buyout, hike-justification. fresh-grad-rebase + refused-advance intentionally NO-OP (kernel already handles both via applyCandidateAnswer side-effects).
- [2026-05-15] Commit 6 — Discovery-sequence rewrite from imperative `getNextDiscoveryQuestion` switch-table to declarative `DISCOVERY_SEQUENCE: Question[]` array with `nextUnanswered(checklist, sequence)` reducer. Cut: cosmetic refactor under hot-path; defer until commit 3's planner consumes it. (NOTE 2026-05-15 session 3: redirected per audit — commit 6 became the phase-transition monotonicity matrix per /tmp/negotiation-flow-audit.md C.4. Shipped 2026-05-15 `eae9c68`. The discovery-sequence rewrite remains deferred under this same line.)
- [2026-05-15] Commit 6 (phase monotonicity) — shipped 2026-05-15 `eae9c68`. `canTransitionPhase(from, to, state)` matrix + derivePhase clamp. Two legitimate backward exceptions: walk-away-reopen (walkAwayReturned flag), verbal-renege (verbalAcceptanceTurn set; allowed regression to counter-offer only). Removed POST_PROBE_PHASES / isPostProbe / alreadyProbed patchwork clauses from derivePhase.
- [2026-05-15] Commit 7 — Voss-tactics scheduler — make labels/mirrors/calibrated-questions fire by a scheduler (turn-cadence + state-trigger) instead of opportunistic in-brief prompt injection. Cut: needs the briefDecisionLog from commit 4 to avoid stacking tactics on the same draft. (Still deferred after session 3.)
- [2026-05-15] Commit 8 — Decision-log surfacing in the post-session coach view — render the (still-deferred) briefDecisionLog as "why the recruiter did X on turn N" timeline. Cut: depends on commits 3+4 landing; surface-only ship. (Still deferred after session 3.)

Session 3 (2026-05-15) shipped commits 6 + 4 in the audit's revised order (6 → 4); commits 7 + 8 remain deferred. Next session: commit 7 (Voss-tactics scheduler) — now unblocked since reactive-followup ledger (commit 4) provides a per-turn de-dupe mechanism that the Voss scheduler can re-use. Commit 8 (coach-view decision-log surfacing) is still surface-only and depends on the deferred briefDecisionLog ring buffer.

## 2026-05-29 realism-pass
- [2026-05-29] P0-1 — humanizer (persona-tic / mid-sentence-hedge / checkback) wire-in initially scoped to `proseReactiveFollowup` only because 34 canonical-string assertions across the suite passed through `renderCanonicalProse`. Cross-arm coverage deferred. (resolved: 2026-05-29 — wired at `renderCanonicalProse` exit point with `HUMANIZER_SUPPRESSED_KINDS` carve-out for terminal-restate / close-recap-formal / walkaway / answer-direct; composition order is `humanize(body)` then prepend `sentimentPrefix`; per-branch wraps in `prose/reactive-followup.ts` removed to avoid double-humanize; planner-level pre-humanize on `answer-direct` retained for the LLM-bypass `deterministicProse` path. `lowercaseFirst` extended with proper-noun guard (vocative regex + `candidateFirstName` threading) so greet-path arms ("Sandeep, take your time …") don't get lowercased post-tic. `responseBankLinter` cartesian extended to include `roundPersona` so highest-precedence overrides are covered. Inline-arm humanizer coverage pinned in `canonicalProseHumanizerExit.test.ts`.)
- [2026-05-29] P0-2 — `candidateRegister` (formal/casual/direct/neutral) inferred from utterance history; threaded into humanizer (rate modulation, formal-tic filtering) and into the response-bank `registerVariants` precedence slot. (resolved: 2026-05-29.)
- [2026-05-29] P1 — `RESPONSE_BANK` linter. Sweeps `(topic × sector × roundPersona × phase × register)` cartesian; reuses canonical `BANNED_RECRUITER_IDIOM_RE` + `META_DIRECTIVE_TOKENS_RE` exports; pins Hinglish allow-list (`ALLOWED_HINGLISH_TOKENS` co-located in `_canonical-prose.ts`); asserts `sectorPhaseOverrides` shadows flat `sectorOverrides` and `registerVariants` shadows `variants/base`. `TOPIC_PROBES: Record<CandidateQuestionTopic, string>` exhaustive map drives the classify→render round-trip without drift risk. (resolved: 2026-05-29.)

## Orphan-detector scope
- [2026-05-15 | 4482f87] `src/__tests__/orphanExports.test.ts` scopes to kernel + negotiation core files. Razorpay/CORS/subscription helpers in `server-handlers/_*.ts` carry ~140 additional "test-only exported" symbols; out of scope for the dead-wiring threat model (they're public API surfaces by design). Widening scope is its own triage ship.

# Tempo design memory

## Design preferences

**Color & type**
- Design direction: clean, simple, modern SaaS aesthetic (Linear, Notion, Stripe), not editorial. The prior editorial/serif-display style with italic/bold copper accent words is retired.
- Never use italics anywhere in the UI — no `<em>`, no `fontStyle: italic`. Use weight/color for emphasis instead.

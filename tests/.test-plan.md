# E2E Test Plan
<!-- Shared team artifact. Remove items from "What to write next" as you claim them. -->

## Quality bar — Definition of Done for this repo

A repo is only at 100% when ALL of these are true:

1. **100% flow coverage.** Every user-facing flow has at least one Tier 1 test.
2. **Functional-flow tests ≥70% of total.** Tier 3 smoke is a safety net only.
3. **Every CRUD surface (create / edit / delete) has ≥1 real UI-driven test.**
4. **Every API mutation has ≥1 end-to-end test.**
5. **ESLint rule forbids `page.waitForTimeout` and `waitForLoadState('networkidle')`.**
6. **Zero admin-seeded "functional" tests.** At least one test per feature drives the UI from zero — no service-role bypass on the happy path.
7. **Cleanup in `afterEach` / `afterAll`.** No orphan data between runs.
8. **CI required check on `main` / `master`.** Branch protection enforces it.
9. **GitHub Action runs the full e2e suite on every PR**, blocks merge on failure.
10. **100% pass rate across 3 consecutive runs.** No flake.

## Tier definitions

- **Tier 1 — Real user journey.** Drives the UI, performs a real mutation, asserts persisted state.
- **Tier 2 — Navigation / visibility.** Loads a page or opens a modal, asserts elements visible, no mutation.
- **Tier 3 — Smoke / crash canary.** Single-assertion page-load test. Safety net only.

## Conventions
- Test directory: `tests/e2e/`
- Framework: Playwright 1.59 (chromium, mobile, tablet, android projects)
- Test environment: `BASE_URL` env or `http://localhost:3000`; `npm run dev` boots via `webServer`
- Auth strategy: **gap — no real test-env login.** Authed specs either `test.skip(!TEST_USER_EMAIL)` or `page.route()`-mock everything.
- Data strategy: **gap — no API seeding helper.** Mutations not exercised.
- Selector convention: mixed — `#id`, `getByRole`, `getByText`, raw CSS. No `data-testid` discipline.
- API client: missing (`tests/helpers/api.ts` not present).
- Auth helper: missing (`tests/helpers/auth.ts` not present).
- Fixtures: missing (`tests/fixtures/base.ts` not present).

## Readiness status

| Check | Status | Notes |
|---|---|---|
| Test environment URL | ⚠️ partial | `BASE_URL` env var used; no `.env.test` template. CI defaults to localhost which is fine, but no staging fallback. |
| Test credentials | ❌ missing | `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` referenced in 30 places but never provisioned. All authed tests silently skip in CI. |
| Seed / reset capability | ❌ missing | No helper to seed users, sessions, or payments. RLS-aware Supabase service-role client not wired. |
| Production safety | ✅ pass | `playwright.config.ts` `baseURL` is localhost; `APP_URL` in `.env.example` points to staging Vercel URL, not used for tests. |

## Existing specs

| File | Tests | T1 | T2 | T3 | Notes |
|---|---:|---:|---:|---:|---|
| `tests/e2e/auth.spec.ts` | 18 | 0 | 16 | 2 | Authed login/logout (2) skip without creds — effectively T2. |
| `tests/e2e/dashboard.spec.ts` | 14 | 0 | 13 | 1 | Authed page-loads (4) skip without creds; only assert heading visible. |
| `tests/e2e/dashboard-authed.spec.ts` | 3 | 0 | 0 | 3 | **Mocked-happy-path anti-pattern.** Mocks Supabase auth, REST, and every `/api/*`. Passes with backend down. Weakest assertion: `expect(typeof hasNextMove).toBe("boolean")`. |
| `tests/e2e/interview.spec.ts` | 14 | 0 | 14 | 0 | Authed setup tests all visibility-only; no session actually started. Salary-negotiation flow not tested. |
| `tests/e2e/landing.spec.ts` | 28 | 0 | 28 | 0 | Heavy `waitForTimeout` use (9 calls). Functional but pure visibility. |
| `tests/e2e/onboarding.spec.ts` | 13 | 0 | 13 | 0 | 11/13 wrapped in `if (visible) { ... }` — silent no-ops when element missing. |
| `tests/e2e/payment.spec.ts` | 22 | 0 | 22 | 0 | **No actual payment flow exercised.** Razorpay order creation, webhook, credit application never hit. |
| `tests/e2e/accessibility.spec.ts` | 20 | 0 | 20 | 0 | Legitimate a11y axe-core coverage. Treat as orthogonal quality gate, not journey coverage. |
| `tests/e2e/visual/public-pages.spec.ts` | 5 | 0 | 5 | 0 | Visual regression on public surfaces. Uses `networkidle` + `waitForTimeout`. |
| `tests/example.spec.ts` | 2 | 0 | 0 | 2 | **Playwright.dev boilerplate. Delete.** |
| **Totals** | **139** | **0** | **131** | **8** | **Tier 1 coverage: 0%.** |

## Coverage report

### Well covered
- *(none)* — every feature has tests, but none reach Tier 1.

### Partially covered
- **Landing page** (`landing.spec.ts`) — visibility, navigation, FAQ accordion. Missing: live "people practiced today" counter assertions, analytics event firing.
- **Auth — signup / login UI** (`auth.spec.ts`) — form rendering, validation, navigation. **Missing: real signup mutation, email verification flow, Google OAuth flow, real login + dashboard land, password reset flow end-to-end, device-token enforcement.**
- **Pricing display** (`payment.spec.ts`) — plan prices, CTAs visible. **Missing: full Razorpay checkout, webhook → credits applied, subscription cancel/pause/reactivate flows, promo code validation.**
- **Onboarding** (`onboarding.spec.ts`) — autocomplete inputs (when visible). **Missing: full 3-step flow, resume upload + parse, profile written to `profiles` table.**
- **Dashboard** (`dashboard.spec.ts`, `dashboard-authed.spec.ts`) — guard redirects + mocked render. **Missing: real authed dashboard with real session history, "Next move" CTA logic, streak rendering.**
- **Interview setup** (`interview.spec.ts`) — visibility of 10 types, role/company autocomplete. **Missing: starting a real session, answering a question (LLM-backed), getting evaluation, viewing report.**

### No tests
- **Resume upload & parse** (`/resume`, `analyze-resume` API) — no tests of upload, AI vs fallback parse, StoredResume narrowing path.
- **Sessions list & detail** (`/sessions`, `/sessions/[id]`) — listing, opening, sharing.
- **Session report sharing** (`share-report` API, `/report/[token]`) — public report token generation, revocation.
- **Analytics page** (`/analytics`) — performance trends, skill scores.
- **Calendar / scheduling** (`/calendar`, `calendar_events` table).
- **Story notebook** (`/notebook`, `story_notebook` table).
- **Settings — profile edit, account delete, data export, subscription management.**
- **Razorpay payment flow end-to-end** — order → checkout (test mode) → webhook → credits applied / tier upgraded.
- **Promo code validation** (`validate-promo`).
- **Referral flow** (`referrals` table, reward issuance).
- **Streak reward issuance** (7 / 14 / 30 milestones).
- **Salary negotiation interview** (`negotiate-turn`, `salary-offer`, `_negotiation-failures.ts` detector).
- **Admin quality routes** (`/admin/*`, `admin-quality-*` endpoints).
- **Account deletion** (`delete-account`, `cleanup-deleted-accounts`).
- **Data export** (`export-user-data` — GDPR).
- **Email verification flow** (`verify-email`, custom_email_verified metadata).
- **Single-device enforcement** (active_device_token rotation).
- **Re-engagement / abandonment / renewal email triggers** (cron endpoints).
- **Marketing — company profile pages** (`/companies/[slug]`), **public profile** (`/profile/[id]`), **blog**, **refund policy**.
- **Mobile-specific** — audio resume after backgrounding (interview mid-session).
- **CSP & service-worker registration smoke.**

## What to write next
<!-- Remove an item when you start writing it. Each item is a ready-to-paste argument for /add-e2e-tests. -->

### P0 — revenue / auth / compliance blockers
1. `Razorpay payment — buy single session credit, webhook applies credit` (complex) → `payment-checkout.spec.ts`
2. `Razorpay payment — subscribe to Starter, webhook upgrades tier` (complex) → `payment-subscription.spec.ts`
3. `Razorpay subscription — cancel / pause / reactivate updates tier in DB` (moderate) → `payment-subscription-mgmt.spec.ts`
4. `Auth — real signup creates row in profiles, sends verify email, redirects to onboarding` (complex) → `auth-signup.spec.ts`
5. `Auth — real login lands on dashboard with real session history` (moderate) → `auth-login.spec.ts`
6. `Auth — password reset email link signs user in and updates password` (complex) → `auth-password-reset.spec.ts`
7. `Auth — single-device enforcement signs out first device when second logs in` (complex) → `auth-device-token.spec.ts`
8. `Account delete — confirms, removes profile, blocks future login with same email` (moderate) → `account-delete.spec.ts`
9. `Data export — GDPR JSON download contains user's sessions + profile` (moderate) → `data-export.spec.ts`

### P1 — core CRUD and multi-step flows
1. `Interview — complete a behavioral session end-to-end, see scored report` (complex) → `interview-behavioral-e2e.spec.ts`
2. `Interview — salary negotiation session writes salary_offer row` (complex) → `interview-negotiation.spec.ts`
3. `Onboarding — 3-step flow uploads resume, picks role/company, lands on dashboard` (complex) → `onboarding-e2e.spec.ts`
4. `Resume — upload PDF, AI parse, edit fields, save` (complex) → `resume.spec.ts`
5. `Sessions list — open a past session, view report, share via public link` (moderate) → `sessions-list.spec.ts`
6. `Dashboard — Next Move CTA targets weakest skill after sessions exist` (moderate) → `dashboard-next-move.spec.ts`
7. `Streak — practicing 7 days in a row issues bonus credit` (moderate) → `streak-reward.spec.ts`
8. `Referral — referee converts to paid, referrer credited` (complex) → `referral-reward.spec.ts`
9. `Settings — edit name + target role, persists across reload` (simple) → `settings-profile.spec.ts`
10. `Promo code — applies discount at checkout; invalid code rejected` (moderate) → `promo-code.spec.ts`
11. `Calendar — create interview-prep event, persists, deletable` (moderate) → `calendar.spec.ts`
12. `Story notebook — add story, edit, delete, persists` (moderate) → `story-notebook.spec.ts`

### P2 — admin, edge cases, role-gated routes, error paths
1. `Free tier — paywall blocks 4th session in same week` (moderate) → `quota-paywall.spec.ts`
2. `Interview — backgrounding the tab and resuming preserves audio context` (complex) → `interview-mobile-audio.spec.ts`
3. `Email verify — clicking expired link surfaces actionable error` (simple) → `auth-verify-expired.spec.ts`
4. `Admin — non-admin user gets 403 on /admin routes and API` (simple) → `admin-rbac.spec.ts`
5. `Rate limit — 11th rapid request returns 429 with retry header` (simple) → `rate-limit.spec.ts`
6. `LLM fallback — Groq down → Gemini answers; both down → static fallback` (complex) → `llm-fallback.spec.ts`
7. `404 — unknown route surfaces branded not-found, link back to home` (simple) → `not-found.spec.ts`
8. `Cookie consent — declining blocks PostHog network calls` (simple) → `cookie-consent.spec.ts`
9. `Offline — banner shows; dashboard renders cached shell` (simple) → `offline.spec.ts`
10. `Public report share — token URL renders read-only report; revoked token 404s` (moderate) → `report-share.spec.ts`
11. `Companies / public profile pages — render and SEO meta present` (simple) → `public-marketing.spec.ts`

## Upgrades needed
- `tests/example.spec.ts:1-18` — **delete entire file**, it's Playwright.dev boilerplate.
- `tests/e2e/dashboard-authed.spec.ts:1-217` — **rewrite as real auth**. Currently mocks every Supabase + API call via `page.route()`. Replace with `helpers/auth.ts` seeded user + real Supabase calls. Today it passes with the backend down.
- `tests/e2e/dashboard-authed.spec.ts:202-207` — `expect(typeof hasNextMove).toBe("boolean")` is a non-assertion; replace with deterministic seed → expected CTA text.
- `tests/e2e/landing.spec.ts:78,92,106,118,130,200,213,226` — 8 `waitForTimeout` calls. Replace with `expect(...).toBeVisible({ timeout })` polling or `waitForFunction`.
- `tests/e2e/payment.spec.ts:6,11,140` — `waitForTimeout` flake risk. Wait on `#pricing` attached + intersection-observer reveal class instead.
- `tests/e2e/visual/public-pages.spec.ts:26-27` — `networkidle` + `waitForTimeout(500)` combo. For visual tests, switch to `page.evaluate(() => document.fonts.ready)` and explicit reveal-class wait.
- `tests/e2e/accessibility.spec.ts:160,210` — `waitForTimeout` for reveal animations. Same fix.
- `tests/e2e/onboarding.spec.ts` — 11 tests wrapped in `if (visible) { ... }`. Either seed deterministic onboarding state and assert unconditionally, or delete the no-op fallbacks.
- `tests/e2e/onboarding.spec.ts`, `tests/e2e/auth.spec.ts`, `tests/e2e/dashboard.spec.ts`, `tests/e2e/interview.spec.ts` — 30 `test.skip(!hasAuth)` sites. Provision a stable test user + helper so these run unconditionally.
- `tests/e2e/auth.spec.ts:8-13,15-20,22-27` — multiple `toBeVisible()`-only assertions after `fill()`. Strengthen to assert the rendered indicator text matches expected strength bucket.
- All authed specs — login is copy-pasted three times. Extract `loginAsTestUser(page)` into `tests/helpers/auth.ts` once a seeded user exists.
- `.github/workflows/ci.yml:80` — runs `npm run dev` for e2e (slow). Switch `playwright.config.ts` `webServer.command` to `npm run build && npm start` and cache `.next/cache`.
- `.github/workflows/ci.yml` — missing Playwright browser cache (`~/.cache/ms-playwright`). Adds 1–2 min per run.
- `.github/workflows/ci.yml` — missing draft-PR skip and 20-min job timeout.
- `playwright.config.ts:10` — `forbidOnly: !!process.env.CI` is good; consider adding `retries: 0` on CI once flake is fixed and reducing from 2.

## Anti-patterns found
- **`page.waitForTimeout` — 16 occurrences** across `landing.spec.ts` (8), `payment.spec.ts` (3), `accessibility.spec.ts` (2), `visual/public-pages.spec.ts` (1), `onboarding`/`dashboard`/`auth` (0 — clean). ESLint rule **not configured** — `eslint.config.js` has no e2e-specific rules. Recommend Chorus-style `no-restricted-syntax` rule scoped to `tests/e2e/**`.
- **`waitForLoadState('networkidle')` — 1 occurrence** at `tests/e2e/visual/public-pages.spec.ts:26`. Flake factory; replace with explicit waits.
- **`page.route()` happy-path mocks — 5 sites** in `tests/e2e/dashboard-authed.spec.ts:87,102,112,124,129`. Test would pass with the entire backend down. Recategorize file as Tier 3 until rewritten.
- **`test.skip(!process.env.TEST_USER_EMAIL...)` — 30 occurrences** across 6 files. Silent skips hide a complete lack of authenticated coverage in CI.
- **`toBeVisible()` as only assertion after mutation — N/A** because no mutations are actually performed today.
- **`if (await el.isVisible()) { expect(el).toBeVisible() }` no-op pattern — 11 occurrences** in `tests/e2e/onboarding.spec.ts:54,67,82,96,110,124,143,158,174,191,193`. Test passes whether the element exists or not.
- **Class-based selectors** — `.mobile-nav-toggle`, `.skip-to-content`, `.particle-canvas` in `landing.spec.ts`, `accessibility.spec.ts`. Brittle to styling changes. Migrate to `data-testid` or `role` selectors.
- **CSS id selector `#signup-email` used for the login form's email field** — `auth.spec.ts:64`, `dashboard.spec.ts:11`, etc. Either rename the id to `#login-email` on the login page or use `getByRole("textbox", { name: /email/i })`.
- **Stale `tests/example.spec.ts`** hitting `https://playwright.dev/` — runs in CI today against a 3rd-party URL.

## Blockers
- **Provision a stable test user** in the test/staging Supabase project. Add `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` to GitHub Actions secrets. Without this, 30 tests silently skip in CI.
- **Add `SUPABASE_SERVICE_ROLE_KEY` for the test project to Actions secrets** so `helpers/api.ts` can seed/clean rows in `profiles`, `sessions`, `payments`, `referrals`, etc. without RLS.
- **Razorpay test-mode credentials** (`RAZORPAY_KEY_ID` / `RAZORPAY_KEY_SECRET` test pair, `RAZORPAY_WEBHOOK_SECRET`) need to be wired in CI for any P0 payment test to run.
- **Mark e2e job as required check** on `main` in branch protection. The workflow exists but isn't blocking.
- **Decide CI target: localhost-built app vs staging deploy.** Recommend localhost-built (`npm run build && npm start`) for determinism + speed.
